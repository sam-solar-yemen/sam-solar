# Sam Solar — Final Merged Build

هذه النسخة هي النسخة المنقحة النهائية من مشروع سام سولار بعد انتقاء العناصر المناسبة من النسخ الثلاث.

## قاعدة الدمج
- البنية الأساسية المتوافقة مع Supabase بقيت هي الأساس.
- تم الاحتفاظ ببنية الجداول الحالية وعدم إعادة تصميم قاعدة البيانات.
- تم الاحتفاظ بـ Auth، لوحة الإدارة، المنتجات، الموردين، المنظومات الجاهزة، العروض، المفضلة، الطلبات والإشعارات والإدارة.
- أضيفت تحسينات واجهة منتقاة من النسخ الأخرى دون إدخال نظام التقييمات والمراجعات.
- أضيفت واجهة سلة مرتبطة بـ `cart_items` مع تعديل الكميات والحذف.
- أضيفت واجهة دخول/إنشاء حساب للعميل باستخدام Supabase Auth.
- لم يتم تغيير فكرة التطبيق أو ترتيب الأقسام الأساسي.

## ملاحظة الاختبار
تمت مراجعة الملفات والربط البرمجي يدويًا. تعذر تنفيذ `npm run build` داخل بيئة العمل الحالية لأن تنزيل/تثبيت `node_modules` لم يكتمل ضمن مهلة التنفيذ؛ لذلك لا أعتبر فشل البناء الحالي حكمًا على المشروع نفسه. يجب تشغيل `npm install` ثم `npm run build` في بيئة التطوير/Bolt.

## البيئة
انسخ `.env.example` إلى `.env` وأدخل:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
- `VITE_ADMIN_EMAILS` اختياريًا لتقييد دخول الإدارة.
