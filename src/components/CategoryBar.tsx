import{BatteryCharging,Sun,Zap,LayoutGrid,Cable,Building2}from'lucide-react';import type{Category}from'@/types';export default function CategoryBar({categories,active,onSelect}:{categories:Category[];active:string|null;onSelect:(id:string|null)=>void}){const icons:any={BatteryCharging,Sun,Zap,LayoutGrid,Cable,Building2};return <div className="flex gap-2 overflow-x-auto no-scrollbar border-b bg-white p-3"><button onClick={()=>onSelect(null)} className={`shrink-0 rounded-full px-4 py-2 text-sm font-bold ${active===null?'bg-amber-500 text-white':'bg-gray-100'}`}>الكل</button>{categories.map(c=>{const I=icons[(c as any).icon||'Sun']||Sun;return <button key={c.id} onClick={()=>onSelect(c.id)} className={`flex shrink-0 items-center gap-1.5 rounded-full px-4 py-2 text-sm font-bold ${active===c.id?'bg-amber-500 text-white':'bg-gray-100'}`}><I size={15}/>{c.name_ar}</button>})}</div>}
