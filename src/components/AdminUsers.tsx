import{useEffect,useState}from'react';import{supabase}from'@/supabase';export default function AdminUsers(){const[u,setU]=useState<any[]>([]);useEffect(()=>{supabase.from('customers').select('*').order('created_at',{ascending:false}).then(({data})=>setU(data||[]))},[]);return <div className="p-5"><h1 className="mb-4 text-2xl font-extrabold">المستخدمون</h1><div className="space-y-2">{u.map(x=><div key={x.id} className="flex justify-between rounded-2xl border bg-white p-4"><span>{x.full_name_ar||x.email||'عميل'}</span><b className="text-amber-600">عميل</b></div>)}</div></div>}
