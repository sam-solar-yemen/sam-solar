import{useState}from'react';import type{Product}from'@/types';import{formatYER}from'@/utils';export default function ProductDetail({product,onBack,onAdd}:{product:Product;onBack:()=>void;onAdd:(p:Product,q:number)=>void}){const[q,setQ]=useState(1);return <div><button onClick={onBack} className="mb-3 text-amber-600">← رجوع</button><div className="overflow-hidden rounded-3xl bg-slate-900 text-white"><img src={product.image_url||'https://placehold.co/800x500?text=Solar'} className="h-64 w-full object-cover"/><div className="p-5"><div className="text-slate-400">{product.brand_ar||''} {product.model?`• ${product.model}`:''}</div><h1 className="text-2xl font-black">{product.name_ar}</h1><div className="mt-3 text-2xl font-black text-amber-400">{formatYER(product.price)} ر.ي</div><p className="mt-4 text-slate-300">{product.description_ar||''}</p><div className="mt-5 grid grid-cols-1 gap-2 sm:grid-cols-3">{[['المواصفات',product.specifications_ar],['الضمان',product.warranty_ar],['التوفر',product.is_available?'متوفر':'غير متوفر']].map(([a,b])=><div key={String(a)} className="rounded-xl bg-slate-800 p-3"><small>{a}</small><div>{b||'—'}</div></div>)}</div><div className="mt-6 flex gap-2"><div className="flex rounded-xl bg-slate-800"><button onClick={()=>setQ(Math.max(1,q-1))} className="px-4">−</button><span className="px-3 py-2">{q}</span><button onClick={()=>setQ(q+1)} className="px-4">+</button></div><button disabled={!product.is_available} onClick={()=>onAdd(product,q)} className="flex-1 rounded-xl bg-amber-400 font-black text-slate-950 disabled:opacity-40">أضف للسلة</button></div></div></div></div>}
