import{useCallback,useEffect,useState}from'react';import{supabase}from'@/supabase';import type{Supplier}from'@/types';export function useSuppliersDB(){const[suppliers,setSuppliers]=useState<Supplier[]>([]);const[loading,setLoading]=useState(true);const fetchSuppliers=useCallback(async()=>{setLoading(true);const{data}=await supabase.from('suppliers').select('*').order('sort_order').order('created_at',{ascending:false});setSuppliers((data||[]) as Supplier[]);setLoading(false)},[]);useEffect(()=>{fetchSuppliers()},[fetchSuppliers]);return{suppliers,loading,fetchSuppliers}}
